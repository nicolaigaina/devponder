import { Link } from "react-scroll";
import styled from "styled-components";
import { DefaultOutlined, DefaultRounded } from "../../styles/common";

export const StyledRoundedLink = styled(Link)`
  ${DefaultRounded}
`;

export const StyledOutlinedLink = styled(Link)`
  ${DefaultOutlined}
`;
